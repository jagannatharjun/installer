#ifndef SHORTCUT_HPP
#define SHORTCUT_HPP

extern "C" int CreateLink(const wchar_t *lpszPathObj, const wchar_t *lpszPathLink,
                          const wchar_t *lpszDesc, const wchar_t *workingDir);

#endif
